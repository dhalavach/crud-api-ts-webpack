export type userData = {
  username: string;
  age: number;
  hobbies: string[] | [];
};

export type optionalUserData = {
  username?: string;
  age?: number;
  hobbies?: string[] | [];
};

export type user = {
  id: string;
  username: string;
  age: number;
  hobbies: string[] | [];
};
